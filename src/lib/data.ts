import type { MenuItem, Discount, Order, CartItem } from './types';

// This file now contains only type definitions and sample data structures for reference.
// All live data is now fetched from and managed by Firebase Firestore.

/**
 * @deprecated Mock data is no longer used. Data is fetched from Firestore.
 */
export const menuItems: MenuItem[] = [];

/**
 * @deprecated Mock data is no longer used. Data is fetched from Firestore.
 */
export const discounts: Discount[] = [];

/**
 * @deprecated Mock data is no longer used. Data is fetched from Firestore.
 */
export const orders: Order[] = [];
